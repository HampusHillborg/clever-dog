workflows:
  android-internal:
    name: Android Internal Testing
    instance_type: linux_x2
    max_build_duration: 60
    environment:
      groups:
        # Vite needs VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY.
        # Keystore signing reads CM_KEYSTORE (base64), CM_KEYSTORE_PASSWORD,
        # CM_KEY_ALIAS, CM_KEY_PASSWORD from this group too.
        - cleverdog_supabase
        # Re-enable once google_play group exists and contains
        # GCLOUD_SERVICE_ACCOUNT_CREDENTIALS (Play Console service account JSON):
        # - google_play
      node: 22
    triggering:
      events: [push]
      branch_patterns:
        - pattern: main
          include: true
    scripts:
      - name: Install Node deps
        script: npm ci --legacy-peer-deps
      - name: Build customer-only web bundle
        script: npm run app:build
      - name: Sync to Android project
        script: npx cap sync android
      - name: Decode upload keystore
        script: |
          echo "$CM_KEYSTORE" | base64 -d > "$CM_BUILD_DIR/cleverdog-upload.jks"
          echo "CM_KEYSTORE_PATH=$CM_BUILD_DIR/cleverdog-upload.jks" >> "$CM_ENV"
      - name: Build signed AAB
        script: |
          cd android
          chmod +x gradlew
          ./gradlew bundleRelease
    artifacts:
      - android/app/build/outputs/bundle/release/*.aab
      - android/app/build/outputs/apk/release/*.apk
    # Re-enable once a Play Console service account JSON is available
    # in the google_play env group as $GCLOUD_SERVICE_ACCOUNT_CREDENTIALS:
    # publishing:
    #   google_play:
    #     credentials: $GCLOUD_SERVICE_ACCOUNT_CREDENTIALS
    #     track: internal

  ios-simulator:
    name: iOS Simulator Build (unsigned)
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      groups:
        - cleverdog_supabase
      xcode: latest
      node: 22
      vars:
        XCODE_PROJECT: "ios/App/App.xcodeproj"
        XCODE_SCHEME: "App"
    # No `triggering` block on purpose — run this from the Codemagic UI
    # via "Start new build" when you want to verify the iOS build compiles.
    scripts:
      - name: Install Node deps
        script: npm ci --legacy-peer-deps
      - name: Build customer-only web bundle
        script: npm run app:build
      - name: Sync Capacitor iOS project
        script: npx cap sync ios
      - name: Generate iOS icon + splash assets
        script: npx capacitor-assets generate --ios
      - name: Resolve Swift Package dependencies
        script: |
          xcodebuild \
            -resolvePackageDependencies \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME"
      - name: Build for iOS Simulator (no signing)
        script: |
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration Debug \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            build
      - name: Package .app bundle
        script: |
          cd build/Build/Products/Debug-iphonesimulator
          zip -r "$CM_BUILD_DIR/App-Simulator.zip" App.app
    artifacts:
      - App-Simulator.zip
      - /tmp/xcodebuild_logs/*.log

  # ─────────────────────────────────────────────────────────────────────
  # ios-testflight är INAKTIV tills Apple Developer Program ($99/år) är på
  # plats. När det är klart, gör så här för att aktivera:
  #   1. Skaffa Apple Developer (developer.apple.com)
  #   2. Skapa app-record i App Store Connect (bundle: se.cleverdog.kundportal)
  #   3. Generera App Store Connect API-key (Users and Access → Keys)
  #   4. I Codemagic UI → Teams → Integrations → App Store Connect:
  #      Lägg in nyckel + key_id + issuer_id. Notera integrationens namn.
  #   5. Avkommentera blocket nedan.
  #   6. Lägg till en `integrations:` sektion under workflow med:
  #          integrations:
  #            app_store_connect: <ditt integration-namn>
  #      (eller byt publishing.auth till "env-vars" och referera $APP_STORE_CONNECT_*-
  #       vars från app_store_connect-env-gruppen)
  #
  # Tills dess: använd `android-internal` (klar) och `ios-simulator` (osignerad,
  # manuellt-triggad) för att verifiera builds.
  # ─────────────────────────────────────────────────────────────────────
  #
  # ios-testflight:
  #   name: iOS TestFlight
  #   instance_type: mac_mini_m2
  #   max_build_duration: 60
  #   environment:
  #     groups:
  #       - app_store_connect
  #       - cleverdog_supabase
  #     ios_signing:
  #       distribution_type: app_store
  #       bundle_identifier: se.cleverdog.kundportal
  #     xcode: latest
  #     cocoapods: default
  #     node: 22
  #   triggering:
  #     events: [push]
  #     branch_patterns:
  #       - pattern: main
  #         include: true
  #   scripts:
  #     - name: Install Node deps
  #       script: npm ci
  #     - name: Build customer-only web bundle
  #       script: npm run app:build
  #     - name: Add iOS platform if missing
  #       script: |
  #         if [ ! -d "ios" ]; then
  #           npx cap add ios
  #         else
  #           npx cap sync ios
  #         fi
  #     - name: Patch Info.plist + entitlements
  #       script: |
  #         chmod +x scripts/patch-ios-config.sh
  #         ./scripts/patch-ios-config.sh
  #     - name: Generate iOS icon + splash assets
  #       script: |
  #         npx capacitor-assets generate --ios
  #     - name: Drop in GoogleService-Info.plist if available
  #       script: |
  #         if [ -n "${GOOGLE_SERVICE_INFO_PLIST:-}" ]; then
  #           echo "$GOOGLE_SERVICE_INFO_PLIST" > ios/App/App/GoogleService-Info.plist
  #           echo "GoogleService-Info.plist installed."
  #         else
  #           echo "GOOGLE_SERVICE_INFO_PLIST not set — push notifications will no-op."
  #         fi
  #     - name: Install CocoaPods
  #       script: |
  #         cd ios/App
  #         pod install
  #     - name: Set up code signing
  #       script: xcode-project use-profiles
  #     - name: Build IPA
  #       script: |
  #         xcode-project build-ipa \
  #           --workspace "$CM_BUILD_DIR/ios/App/App.xcworkspace" \
  #           --scheme "App"
  #   artifacts:
  #     - build/ios/ipa/*.ipa
  #     - /tmp/xcodebuild_logs/*.log
  #   publishing:
  #     app_store_connect:
  #       auth: integration
  #       submit_to_testflight: true
  #       beta_groups:
  #         - Internal Testers
